public class Process 
{
	int faza, remaining;

	public Process(int _faza)
	{
		remaining = faza = _faza;
	}

	public int compareTo(Process p)
	{
		if(remaining == p.remaining)
			return 0;
		if(remaining > p.remaining)
			return 1;
		return -1; 
	}
}
